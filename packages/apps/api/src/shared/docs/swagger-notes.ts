/**
 * Shared Swagger description snippets to keep API docs wording consistent.
 */
export const DEMO_AUTH_SCOPE_NOTE =
  'Demo scope: authenticated users share one global workspace (no per-user ownership isolation).';
